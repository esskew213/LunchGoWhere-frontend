import React from 'react';
import { Typography } from '@mui/material';
const Landing = () => {
	return (
		<div>
			<Typography variant="h1">LUNCH GO WHERE</Typography>
		</div>
	);
};

export default Landing;
